```python showLineNumbers copy filename="Put Anything Ignored Generation usage example"
import sdk
from sdk.models import shared

s = sdk.SDK(
    security=shared.Security(
        api_key_auth="Token YOUR_API_KEY",
    ),
    global_path_param=100,
    global_query_param='some example global query param',
)

req = 'quantify'

res = s.sdk.put_anything_ignored_generation(req)

if res.put_anything_ignored_generation_200_application_json_object is not None:
    # handle response
```
