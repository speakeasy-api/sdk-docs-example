```typescript showLineNumbers copy filename="Request Body Post Json Data Types Int32 usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyPostJsonDataTypesInt32(475289);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
