```typescript showLineNumbers copy filename="Nullable Required Shared Object Post usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.nullableRequiredSharedObjectPost({
    nullableOptionalObj: {
      required: 45614,
    },
    nullableRequiredObj: {
      required: 961937,
    },
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
