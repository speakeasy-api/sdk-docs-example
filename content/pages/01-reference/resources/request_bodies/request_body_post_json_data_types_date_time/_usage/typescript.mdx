```typescript showLineNumbers copy filename="Request Body Post Json Data Types Date Time usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyPostJsonDataTypesDateTime(new Date("2021-03-26T06:24:55.249Z"));

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
