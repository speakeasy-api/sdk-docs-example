```typescript showLineNumbers copy filename="Request Body Put Multipart Different File Name usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyPutMultipartDifferentFileName({
    differentFileName: {
      content: "`*Yx*q)K&?" as bytes <<<>>>,
      differentFileName: "Gorgeous",
    },
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
