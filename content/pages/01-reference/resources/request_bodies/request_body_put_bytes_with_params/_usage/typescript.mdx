```typescript showLineNumbers copy filename="Request Body Put Bytes With Params usage example"
import { SDK } from "openapi";
import { RequestBodyPutBytesWithParamsRequest } from "openapi/dist/sdk/models/operations";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });
const requestBody: Uint8Array = ",BJBE\"R2oF" as bytes <<<>>>;
const queryStringParam: string = "capacitor";

  const res = await sdk.requestBodies.requestBodyPutBytesWithParams(requestBody, queryStringParam);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
