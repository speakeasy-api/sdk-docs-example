```typescript showLineNumbers copy filename="Delete Resource usage example"
import { SDK } from "openapi";
import { DeleteResourceRequest } from "openapi/dist/sdk/models/operations";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });
const resourceId: string = "pace";

  const res = await sdk.resource.deleteResource(resourceId);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
