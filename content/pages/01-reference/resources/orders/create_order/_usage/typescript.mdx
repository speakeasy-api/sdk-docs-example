```typescript CreateOrder.ts
import { Speakeasybar } from "speakeasybar";
import { CreateOrderOrderUpdateRequestBody, CreateOrderOrderUpdateResponse } from "speakeasybar/dist/models/callbacks";
import { ErrorT, OrderInput, OrderType } from "speakeasybar/dist/models/components";
import { CreateOrderRequest } from "speakeasybar/dist/models/operations";

(async() => {
  const sdk = new Speakeasybar({
    apiKey: "",
  });
const requestBody: OrderInput[] = [
  {
    productCode: "NAC-3F2D1",
    quantity: 715190,
    type: OrderType.Ingredient,
  },
];
const callbackUrl: string = "string";

  const res = await sdk.orders.createOrder(requestBody, callbackUrl);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
