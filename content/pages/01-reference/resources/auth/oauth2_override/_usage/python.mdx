```python showLineNumbers copy filename="Oauth2 Override usage example"
import sdk
from sdk.models import operations

s = sdk.SDK(
    global_path_param=100,
    global_query_param='some example global query param',
)


res = s.auth.oauth2_override("Bearer YOUR_OAUTH2_TOKEN")

if res.token is not None:
    # handle response
```
