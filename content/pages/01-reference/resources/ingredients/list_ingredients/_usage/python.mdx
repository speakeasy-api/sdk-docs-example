```python ListIngredients.py
import sdk
from sdk.models import operations, shared

s = sdk.SDK(
    api_key="",
)


res = s.ingredients.list_ingredients(ingredients=[
    'string',
])

if res.ingredients is not None:
    # handle response
    pass
```
