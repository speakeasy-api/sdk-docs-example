```csharp ListDrinks.cs
using SDK;
using SDK.Models.Shared;
using SDK.Models.Operations;

var sdk = new SDKSDK(
    security: new Security() {
        ApiKey = "",
    }
);

using(var res = await sdk.Drinks.ListDrinksAsync(SDK.Models.Shared.DrinkType.Wine))
{
    // handle response
}
```
