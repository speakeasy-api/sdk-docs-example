```go ListDrinks.go
package main

import(
	"context"
	"log"
	templatesdk "github.com/speakeasy-sdks/template-sdk"
	"github.com/speakeasy-sdks/template-sdk/pkg/models/shared"
)

func main() {
    s := templatesdk.New(
        templatesdk.WithSecurity(""),
    )


    var drinkType *shared.DrinkType = shared.DrinkTypeWine

    ctx := context.Background()
    res, err := s.Drinks.ListDrinks(ctx, drinkType)
    if err != nil {
        log.Fatal(err)
    }

    if res.Drinks != nil {
        // handle response
    }
}
```
