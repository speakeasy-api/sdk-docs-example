```typescript GetDrink.ts
import { SDK } from "openapi";
import { GetDrinkRequest } from "openapi/dist/sdk/models/operations";

(async() => {
  const sdk = new SDK({
    apiKey: "",
  });
const name: string = "quantify";

  const res = await sdk.drinks.getDrink(name);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
