
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "5"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript GetDrink.ts
  import { Speakeasybar } from "speakeasybar";
import { GetDrinkRequest } from "speakeasybar/dist/models/operations";

(async() => {
  const sdk = new Speakeasybar({
    apiKey: "",
  });
const name: string = "string";

  const res = await sdk.drinks.getDrink(name);

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "code": "string",
    "details": {},
    "message": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>
