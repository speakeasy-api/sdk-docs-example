
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "5"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```go Authenticate.go
  package main

import(
	"github.com/speakeasy-sdks/template-sdk/models/components"
	templatesdk "github.com/speakeasy-sdks/template-sdk"
	"context"
	"github.com/speakeasy-sdks/template-sdk/models/operations"
	"log"
)

func main() {
    s := templatesdk.New(
        templatesdk.WithSecurity(""),
    )

    ctx := context.Background()
    res, err := s.Authentication.Authenticate(ctx, operations.AuthenticateRequestBody{})
    if err != nil {
        log.Fatal(err)
    }

    if res.Object != nil {
        // handle response
    }
}
  ```
  ---

  ```json Example Response
  {
    "code": "string",
    "details": {},
    "message": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>
