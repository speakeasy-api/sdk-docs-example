```typescript showLineNumbers copy filename="Deprecated Operation With Comments Get usage example"
import { SDK } from "openapi";
import { DeprecatedOperationWithCommentsGetRequest } from "openapi/dist/sdk/models/operations";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });
const deprecatedParameter: string = "Rutherfordium";
const newParameter: string = "Concrete";

  const res = await sdk.generation.deprecatedOperationWithCommentsGet(deprecatedParameter, newParameter);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
