```python showLineNumbers copy filename="Ignores Post usage example"
import sdk
from sdk.models import operations, shared

s = sdk.SDK(
    security=shared.Security(
        api_key_auth="Token YOUR_API_KEY",
    ),
    global_path_param=100,
    global_query_param='some example global query param',
)


res = s.generation.ignores_post(request_body=operations.IgnoresPostApplicationJSON(), test_param='Sausages')

if res.http_bin_simple_json_object is not None:
    # handle response
```
