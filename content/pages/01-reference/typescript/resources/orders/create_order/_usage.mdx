
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "5"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript CreateOrder.ts
  import { SDK } from "openapi";
import { CreateOrderOrderUpdateRequestBody, CreateOrderOrderUpdateResponse } from "openapi/dist/models/callbacks";
import { OrderInput, OrderType } from "openapi/dist/models/components";
import { CreateOrderRequest } from "openapi/dist/models/operations";

async function run() {
  const sdk = new SDK({
    apiKey: "",
  });
const requestBody: OrderInput[] = [
  {
    type: OrderType.Ingredient,
    productCode: "APM-1F2D3",
    quantity: 844266,
  },
];
const callbackUrl: string = "string";

  const res = await sdk.orders.createOrder(requestBody, callbackUrl);

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {
    "code": "string",
    "message": "string",
    "details": {}
  }
  ```
  </CH.Code>
</WithStatusBar>
