
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "5"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```csharp Authenticate.cs
  using Openapi;
using Openapi.Models.Components;
using Openapi.Models.Requests;

var sdk = new SDK(
    security: new Security() {
        ApiKey = "",
    });

AuthenticateRequestBody req = new AuthenticateRequestBody() {};

using(var res = await sdk.Authentication.AuthenticateAsync(req))
{

    // handle response
}
  ```
  ---

  ```json Example Response
  {
    "code": "string",
    "message": "string",
    "details": {}
  }
  ```
  </CH.Code>
</WithStatusBar>
