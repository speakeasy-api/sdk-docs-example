```go showLineNumbers copy
package main

import(
	"context"
	"log"
	"github.com/speakeasy-sdks/petstore-test-go"
)

func main() {
    s := petstore.New()

    ctx := context.Background()
    res, err := s.Pets.CreatePets(ctx)
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```