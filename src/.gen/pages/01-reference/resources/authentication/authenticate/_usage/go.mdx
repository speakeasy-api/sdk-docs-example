```go Authenticate.go
package main

import(
	"context"
	"log"
	templatesdk "github.com/speakeasy-sdks/template-sdk"
	"github.com/speakeasy-sdks/template-sdk/pkg/models/shared"
	"github.com/speakeasy-sdks/template-sdk/pkg/models/operations"
)

func main() {
    s := templatesdk.New(
        templatesdk.WithSecurity(""),
    )

    ctx := context.Background()
    res, err := s.Authentication.Authenticate(ctx, operations.AuthenticateRequestBody{})
    if err != nil {
        log.Fatal(err)
    }

    if res.Object != nil {
        // handle response
    }
}
```
