```typescript SubscribeToWebhooks.ts
import { SDK } from "openapi";
import { SubscribeToWebhooksRequestBodyWebhook } from "openapi/dist/sdk/models/operations";

(async() => {
  const sdk = new SDK({
    apiKey: "",
  });

  const res = await sdk.config.subscribeToWebhooks([
    {},
  ]);


  if (res.statusCode == 200) {
    // handle response
  }
})();
```
