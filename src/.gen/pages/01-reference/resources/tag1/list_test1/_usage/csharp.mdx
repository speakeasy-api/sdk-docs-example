```csharp ListTest1.cs
using Openapi;
using Openapi.Models.Operations;

var sdk = new SDK(
    queryParam1: "some example query param"
);

var res = await sdk.ListTest1Async(new ListTest1Security() {
    ApiKey = "<YOUR_API_KEY>",
}, "some example header param", 100, "some example query param");

// handle response
```
