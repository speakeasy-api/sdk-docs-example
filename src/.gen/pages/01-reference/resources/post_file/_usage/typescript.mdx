```typescript PostFile.ts
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    apiKey: "<YOUR_API_KEY>",
    queryParam1: "some example query param",
  });

  const res = await sdk.postFile({
    file: {
      content: "TXcoYpSoHZ" as bytes <<<>>>,
      fileName: "visionary_venezuela.wav",
    },
  });


  if (res.statusCode == 200) {
    // handle response
  }
})();
```
