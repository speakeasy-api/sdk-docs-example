```python GetDrink.py
import sdk
from sdk.models import operations, shared

s = sdk.SDK(
    api_key="",
)


res = s.drinks.get_drink(name='string')

if res.drink is not None:
    # handle response
    pass
```
