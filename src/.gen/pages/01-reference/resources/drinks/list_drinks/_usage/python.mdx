```python ListDrinks.py
import sdk
from sdk.models import operations, shared

s = sdk.SDK(
    api_key="",
)


res = s.drinks.list_drinks(drink_type=shared.DrinkType.WINE)

if res.drinks is not None:
    # handle response
    pass
```
