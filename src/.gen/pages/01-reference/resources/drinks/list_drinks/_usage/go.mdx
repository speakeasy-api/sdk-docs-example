```go ListDrinks.go
package main

import(
	"context"
	"log"
	templatesdk "github.com/speakeasy-sdks/template-sdk"
	"github.com/speakeasy-sdks/template-sdk/models/components"
)

func main() {
    s := templatesdk.New(
        templatesdk.WithSecurity(""),
    )


    var drinkType *components.DrinkType = components.DrinkTypeWine

    ctx := context.Background()
    res, err := s.Drinks.ListDrinks(ctx, drinkType)
    if err != nil {
        log.Fatal(err)
    }

    if res.Classes != nil {
        // handle response
    }
}
```
