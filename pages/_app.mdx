import { createElement } from 'react';
import { Inter, IBM_Plex_Mono } from "next/font/google";
import styles from '@/styles/index.scss'; // Assuming this is the correct path to your SCSS file

export const inter = Inter({
  subsets: ['latin'],
  variable: '--primary-font',
  display: 'swap',
});

export const ibmPlexMono = IBM_Plex_Mono({
  subsets: ['latin'],
  weight: ["100", "200", "300", "400", "500", "600", "700"],
  variable: '--mono-font',
  display: 'swap',
});

import { ScrollManager, MultiPageContext } from 'components/scrollManager';
import { LanguageProvider } from '@/content/languages';

import "@code-hike/mdx/dist/index.css";

export default function App(props) {
  const { Component, pageProps } = props;
  return (
    <div className={`${inter.variable} ${ibmPlexMono.variable}`}>
      <MultiPageContext.Provider value={ false }>
        <ScrollManager>
          <LanguageProvider>
            <Component {...pageProps} />
          </LanguageProvider>
        </ScrollManager>
      </MultiPageContext.Provider>
    </div>
  )
}
