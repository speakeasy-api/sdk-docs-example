```typescript showLineNumbers copy filename="Create File usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.resource.createFile({
    file: {
      content: ";'7`^D7[^w" as bytes <<<>>>,
      file: "Cab index",
    },
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
