```typescript showLineNumbers copy filename="Multiple Simple Scheme Auth usage example"
import { SDK } from "openapi";
import { MultipleSimpleSchemeAuthSecurity } from "openapi/dist/sdk/models/operations";

(async() => {
  const sdk = new SDK({
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });
const operationSecurity: MultipleSimpleSchemeAuthSecurity = {
  apiKeyAuthNew: "Token <YOUR_API_KEY>",
  oauth2: "Bearer YOUR_OAUTH2_TOKEN",
};

  const res = await sdk.authNew.multipleSimpleSchemeAuth({
    basicAuth: {
      password: "GcDLHesCr6iCzfh",
      username: "Celestine.Hayes",
    },
    headerAuth: [
      {
        expectedValue: "channels",
        headerName: "North",
      },
    ],
  }, operationSecurity);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
