```python showLineNumbers copy filename="Oauth2 Auth New usage example"
import sdk
from sdk.models import operations, shared

s = sdk.SDK(
    global_path_param=100,
    global_query_param='some example global query param',
)

req = shared.AuthServiceRequestBody(
    basic_auth=shared.AuthServiceRequestBodyBasicAuth(
        password='lM6tAzyZcYk0PWc',
        username='David.Zemlak61',
    ),
    header_auth=[
        shared.AuthServiceRequestBodyHeaderAuth(
            expected_value='bus Reggae Flerovium',
            header_name='index',
        ),
    ],
)

res = s.auth_new.oauth2_auth_new(req, "Bearer YOUR_OAUTH2_TOKEN")

if res.status_code == 200:
    # handle response
```
