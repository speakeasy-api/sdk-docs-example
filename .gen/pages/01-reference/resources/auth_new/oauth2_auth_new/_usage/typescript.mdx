```typescript showLineNumbers copy filename="Oauth2 Auth New usage example"
import { SDK } from "openapi";
import { Oauth2AuthNewSecurity } from "openapi/dist/sdk/models/operations";

(async() => {
  const sdk = new SDK({
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });
const operationSecurity: Oauth2AuthNewSecurity = "Bearer YOUR_OAUTH2_TOKEN";

  const res = await sdk.authNew.oauth2AuthNew({
    basicAuth: {
      password: "lM6tAzyZcYk0PWc",
      username: "David.Zemlak61",
    },
    headerAuth: [
      {
        expectedValue: "bus Reggae Flerovium",
        headerName: "index",
      },
    ],
  }, operationSecurity);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
