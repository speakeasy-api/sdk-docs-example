```python showLineNumbers copy filename="Request Body Put String With Params usage example"
import sdk
from sdk.models import operations, shared

s = sdk.SDK(
    security=shared.Security(
        api_key_auth="Token YOUR_API_KEY",
    ),
    global_path_param=100,
    global_query_param='some example global query param',
)


res = s.request_bodies.request_body_put_string_with_params(request_body='red', query_string_param='orange')

if res.res is not None:
    # handle response
```
