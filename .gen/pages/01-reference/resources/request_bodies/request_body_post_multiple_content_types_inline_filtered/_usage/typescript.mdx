```typescript showLineNumbers copy filename="Request Body Post Multiple Content Types Inline Filtered usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyPostMultipleContentTypesInlineFiltered({
    bool: false,
    num: 9039.84,
    str: "bypass invoice",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
