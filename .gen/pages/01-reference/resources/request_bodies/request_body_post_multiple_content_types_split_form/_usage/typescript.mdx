```typescript showLineNumbers copy filename="Request Body Post Multiple Content Types Split Form usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyPostMultipleContentTypesSplitForm({
    bool3: false,
    num3: 2400.2,
    str3: "red orange curtailment",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
