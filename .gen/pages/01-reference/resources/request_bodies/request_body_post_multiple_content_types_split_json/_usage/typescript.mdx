```typescript showLineNumbers copy filename="Request Body Post Multiple Content Types Split Json usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyPostMultipleContentTypesSplitJson({
    bool: false,
    num: 4581.39,
    str: "Rap Minivan",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
