```typescript showLineNumbers copy filename="Request Body Put Multipart File usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyPutMultipartFile({
    file: {
      content: "Zo$Q\"PH]'S" as bytes <<<>>>,
      file: "City",
    },
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
