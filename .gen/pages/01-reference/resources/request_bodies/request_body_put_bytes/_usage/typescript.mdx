```typescript showLineNumbers copy filename="Request Body Put Bytes usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyPutBytes("xHJ9dof$=z" as bytes <<<>>>);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
