```typescript showLineNumbers copy filename="Request Body Write Only Output usage example"
import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.requestBodies.requestBodyWriteOnlyOutput({
    bool: false,
    num: 6387.62,
    string: "bluetooth frank Bedfordshire",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
