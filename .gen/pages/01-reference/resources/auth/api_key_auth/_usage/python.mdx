```python showLineNumbers copy filename="Api Key Auth usage example"
import sdk
from sdk.models import operations

s = sdk.SDK(
    global_path_param=100,
    global_query_param='some example global query param',
)


res = s.auth.api_key_auth("Token YOUR_API_KEY")

if res.token is not None:
    # handle response
```
