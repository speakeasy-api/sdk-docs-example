```typescript showLineNumbers copy filename="Oauth2 Override usage example"
import { SDK } from "openapi";
import { Oauth2OverrideRequest, Oauth2OverrideSecurity } from "openapi/dist/sdk/models/operations";

(async() => {
  const sdk = new SDK({
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });
const operationSecurity: Oauth2OverrideSecurity = "Bearer YOUR_OAUTH2_TOKEN";

  const res = await sdk.auth.oauth2Override(operationSecurity);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
