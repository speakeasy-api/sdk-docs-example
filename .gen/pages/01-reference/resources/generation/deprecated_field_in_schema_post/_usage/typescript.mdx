```typescript showLineNumbers copy filename="Deprecated Field In Schema Post usage example"
import { SDK } from "openapi";
import { DeprecatedFieldInObjectDeprecatedEnum } from "openapi/dist/sdk/models/shared";

(async() => {
  const sdk = new SDK({
    security: {
      apiKeyAuth: "Token YOUR_API_KEY",
    },
    globalPathParam: 100,
    globalQueryParam: "some example global query param",
  });

  const res = await sdk.generation.deprecatedFieldInSchemaPost({});

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
